server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vinicius-lds/microservices_do_0_com_spring_cloud_spring_boot_e_docker
          default-label: main
          search-paths: 
          - 'config-repo*'
          - 'config-repo/greeting-service*'
